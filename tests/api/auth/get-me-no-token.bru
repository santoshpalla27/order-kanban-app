meta {
  name: Get Me - No Token (401)
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/auth/me
  body: none
  auth: none
}

tests {
  test("status is 401", function() {
    expect(res.status).to.equal(401);
  });
}
